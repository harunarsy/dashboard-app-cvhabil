{
  "name": "@habil/core",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "prepare": "npm run build",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "comment": "typescript sengaja di dependencies, BUKAN devDependencies: npm tidak memasang devDependencies milik dependensi file:, sehingga script prepare gagal 'tsc: command not found' saat build Vercel. typescript hanya dipakai saat build, tidak pernah diimpor kode aplikasi.",
  "dependencies": {
    "typescript": "^5.7.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
