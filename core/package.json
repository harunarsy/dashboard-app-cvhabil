{
  "name": "@habil/core",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "comment": "TIDAK ADA script prepare. Vercel membangun frontend/ dgn Root Directory frontend/, dan core/ ada di sebelahnya — typescript tidak pernah terlihat dari core/, jadi prepare selalu gagal 'tsc: command not found'. Karena itu core/dist ikut ter-commit dan Vercel tidak membangun apa pun. WAJIB: jalankan `npm run build` di core/ lalu commit dist SETIAP kali mengubah core/src.",
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
